fun fib(n) {
    if (n <= 1) {
        return n;
    }

    var first = fib(n - 2);
    var second = fib(n - 1);

    print first;

    // return fib(n - 2) + fib(n - 1);
    print first + second;
}

print fib(5);
// for (var i = 0; i < 20; i = i + 1) {
   // print fib(i);
// }